<header>
  <div class="container d-flex h-100 align-items-center">
    <div class="mx-auto text-center">
      <h1 class="mx-auto my-0 text-uppercase">共有カレンダー</h1>
      <h3 class="mx-auto mt-3 mb-5">仲間たちとシフトを共有することができます。<br/>一緒に成長を実感しましょう。</h3>
      <% if user_signed_in? %>
        <%= link_to "Myページ", user_path(current_user.id), class: "btn btn-primary js-scroll-trigger" %>
        <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-primary js-scroll-trigger" %>
      <% else %>
        <%= form_with( model: User, url: user_session_path, local: true )do |f|%>
          <%= f.hidden_field :email, value: "test@example.com" %>
          <%= f.hidden_field :password, value: "password" %>
          <%= f.submit "テストユーザーでログイン", class: "btn btn-primary js-scroll-trigger" %>
        <% end %>
        <br/>
        <%= link_to "ログイン", login_path , class: "btn btn-info js-scroll-trigger" %>
        <%= link_to "新規登録", sign_up_path, class: "btn btn-info js-scroll-trigger" %>
      <% end %>
    </div>
  </div>
</header>